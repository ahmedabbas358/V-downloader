diff --git a/app/src/main/java/com/junkfood/seal/download/DownloaderV2.kt b/app/src/main/java/com/junkfood/seal/download/DownloaderV2.kt
index dc0d2c66..88acb65f 100644
--- a/app/src/main/java/com/junkfood/seal/download/DownloaderV2.kt
+++ b/app/src/main/java/com/junkfood/seal/download/DownloaderV2.kt
@@ -475,7 +475,9 @@ class DownloaderV2Impl(
                         }
                     )
                     .mapCatching { pathList ->
-                        if (!task.preferences.skipDownload) {
+                        val isSubtitleOnly = task.preferences.skipDownload && task.preferences.downloadSubtitle
+                        // Always validate the file exists and is not 0 bytes, unless it's a dry run where NO files are expected
+                        if (!task.preferences.skipDownload || isSubtitleOnly) {
                             if (pathList.isEmpty()) {
                                 throw Exception("No files were downloaded. yt-dlp may have skipped or failed.")
                             }
diff --git a/app/src/main/java/com/junkfood/seal/ui/page/downloadv2/DownloadPageV2.kt b/app/src/main/java/com/junkfood/seal/ui/page/downloadv2/DownloadPageV2.kt
index bc7ad28e..70925932 100644
--- a/app/src/main/java/com/junkfood/seal/ui/page/downloadv2/DownloadPageV2.kt
+++ b/app/src/main/java/com/junkfood/seal/ui/page/downloadv2/DownloadPageV2.kt
@@ -379,6 +379,24 @@ fun DownloadPageImplV2(
                 Column(modifier = Modifier.fillMaxWidth()) {
                     Spacer(Modifier.height(with(LocalDensity.current) { headerOffset.toDp() }))
                     Header(onMenuOpen = onMenuOpen, modifier = Modifier.padding(horizontal = 16.dp))
+                    
+                    if (com.junkfood.seal.util.PreferenceUtil.getString(com.junkfood.seal.util.YT_DLP_VERSION).isEmpty()) {
+                        androidx.compose.material3.Surface(
+                            color = MaterialTheme.colorScheme.errorContainer,
+                            modifier = Modifier
+                                .fillMaxWidth()
+                                .padding(horizontal = 20.dp, vertical = 8.dp),
+                            shape = MaterialTheme.shapes.medium
+                        ) {
+                            Text(
+                                text = stringResource(R.string.ytdlp_missing_alert),
+                                color = MaterialTheme.colorScheme.onErrorContainer,
+                                modifier = Modifier.padding(16.dp),
+                                style = MaterialTheme.typography.bodyMedium
+                            )
+                        }
+                    }
+
                     SelectionGroupRow(
                         modifier =
                             Modifier.horizontalScroll(rememberScrollState())
diff --git a/app/src/main/java/com/junkfood/seal/ui/page/downloadv2/configure/FormatPage.kt b/app/src/main/java/com/junkfood/seal/ui/page/downloadv2/configure/FormatPage.kt
index 44c2e50a..b01050d0 100644
--- a/app/src/main/java/com/junkfood/seal/ui/page/downloadv2/configure/FormatPage.kt
+++ b/app/src/main/java/com/junkfood/seal/ui/page/downloadv2/configure/FormatPage.kt
@@ -149,7 +149,7 @@ fun FormatPage(
 
     var diffSubtitleLanguages by remember { mutableStateOf(emptySet<String>()) }
 
-    val isSubtitleOnly = com.junkfood.seal.util.PreferenceUtil.getDownloadType() == com.junkfood.seal.download.DownloadType.Subtitle
+    val isSubtitleOnly = com.junkfood.seal.util.PreferenceUtil.getDownloadType() == com.junkfood.seal.util.DownloadType.Subtitle
 
     FormatPageImpl(
         modifier = modifier,
diff --git a/app/src/main/java/com/junkfood/seal/util/DownloadUtil.kt b/app/src/main/java/com/junkfood/seal/util/DownloadUtil.kt
index ec369073..c182ba42 100644
--- a/app/src/main/java/com/junkfood/seal/util/DownloadUtil.kt
+++ b/app/src/main/java/com/junkfood/seal/util/DownloadUtil.kt
@@ -189,6 +189,10 @@ object DownloadUtil {
                     } else {
                         addOption("--dump-single-json")
                     }
+                    if (extractAudio) {
+                        addOption("--extract-audio")
+                    }
+                    addOption("--no-abort-on-error")
                     addYoutubeCompatibilityOptions()
                     addResilienceOptions(light = true)
                     if (playlistIndex == null) {
@@ -813,12 +817,12 @@ object DownloadUtil {
                     addOption("--compat-options", "no-youtube-unavailable-videos,no-live-chat")
                     addResilienceOptions()
 
-                    if (playlistItem != 0 && downloadPlaylist) {
+                    if (playlistItem != 0 && (downloadPlaylist || isFallback)) {
                         if (!isFallback) {
                             addOption("--playlist-items", playlistItem)
                             if (subdirectoryPlaylistTitle && !videoInfo.playlist.isNullOrEmpty()) {
                                 if (skipDownload && downloadSubtitle) {
-                                    outputBuilder.append("Sub ")
+                                    outputBuilder.append("sub_")
                                 }
                                 outputBuilder.append(PLAYLIST_TITLE_SUBDIRECTORY_PREFIX)
                             }
@@ -830,7 +834,7 @@ object DownloadUtil {
                             addOption("--parse-metadata", "$playlistItem:%(playlist_index)s")
                             if (subdirectoryPlaylistTitle && fallbackPlaylistTitle.isNotEmpty()) {
                                 if (skipDownload && downloadSubtitle) {
-                                    outputBuilder.append("Sub ")
+                                    outputBuilder.append("sub_")
                                 }
                                 outputBuilder.append(PLAYLIST_TITLE_SUBDIRECTORY_PREFIX)
                             }
@@ -977,6 +981,7 @@ object DownloadUtil {
                 FileUtil.scanFileToMediaLibraryPostDownload(
                         title = fileName,
                         downloadDir = downloadPath,
+                        isSubtitleOnly = skipDownload && downloadSubtitle
                     )
                     .run {
                         if (privateMode) Result.success(emptyList())
diff --git a/app/src/main/java/com/junkfood/seal/util/FileUtil.kt b/app/src/main/java/com/junkfood/seal/util/FileUtil.kt
index 78d502c4..b552315c 100644
--- a/app/src/main/java/com/junkfood/seal/util/FileUtil.kt
+++ b/app/src/main/java/com/junkfood/seal/util/FileUtil.kt
@@ -104,16 +104,18 @@ object FileUtil {
         }
 
     @CheckResult
-    fun scanFileToMediaLibraryPostDownload(title: String, downloadDir: String): List<String> =
+    fun scanFileToMediaLibraryPostDownload(title: String, downloadDir: String, isSubtitleOnly: Boolean = false): List<String> =
         File(downloadDir)
             .walkTopDown()
             .filter { it.isFile && it.absolutePath.contains(title) }
             .map { it.absolutePath }
             .toMutableList()
             .apply {
-                MediaScannerConnection.scanFile(context, this.toList().toTypedArray(), null, null)
-                removeAll {
-                    it.contains(Regex(THUMBNAIL_REGEX)) || it.contains(Regex(SUBTITLE_REGEX))
+                if (!isSubtitleOnly) {
+                    MediaScannerConnection.scanFile(context, this.toList().toTypedArray(), null, null)
+                    removeAll {
+                        it.contains(Regex(THUMBNAIL_REGEX)) || it.contains(Regex(SUBTITLE_REGEX))
+                    }
                 }
             }
 
diff --git a/app/src/main/java/com/junkfood/seal/util/VideoInfo.kt b/app/src/main/java/com/junkfood/seal/util/VideoInfo.kt
index afb3b383..9b42050c 100644
--- a/app/src/main/java/com/junkfood/seal/util/VideoInfo.kt
+++ b/app/src/main/java/com/junkfood/seal/util/VideoInfo.kt
@@ -67,6 +67,7 @@ data class VideoInfo(
     val abr: Double? = null,
     val asr: Int? = null,
     val epoch: Int? = null,
+    val url: String? = null,
     @SerialName("requested_downloads") val requestedDownloads: List<RequestedDownload>? = null,
     @SerialName("requested_formats") val requestedFormats: List<Format>? = null,
     val filename: String? = null,
@@ -79,10 +80,37 @@ data class VideoInfo(
      * This NEVER invents formats that aren't present in the original JSON ΓÇö it only
      * filters out junk entries (e.g. "storyboard" mhtml formats, formats with no url).
      */
-    fun availableFormats(): List<Format> =
-        formats.orEmpty().filter { f ->
+    fun availableFormats(): List<Format> {
+        val parsedFormats = formats.orEmpty().filter { f ->
             !f.url.isNullOrBlank() && !f.isStoryboard()
         }
+        if (parsedFormats.isNotEmpty()) return parsedFormats
+
+        // Fallback for some social media sites (e.g. TikTok, Instagram) where yt-dlp returns a single direct URL
+        // at the root level and the 'formats' array is missing or empty.
+        if (!url.isNullOrBlank()) {
+            return listOf(
+                Format(
+                    formatId = formatId ?: "0",
+                    url = url,
+                    ext = ext,
+                    formatNote = formatNote,
+                    fileSizeApprox = fileSizeApprox,
+                    fileSize = fileSize,
+                    tbr = tbr,
+                    width = width,
+                    height = height,
+                    resolution = resolution,
+                    fps = fps,
+                    vcodec = vcodec ?: "unknown",
+                    acodec = acodec ?: "unknown",
+                    vbr = vbr,
+                    abr = abr
+                )
+            )
+        }
+        return emptyList()
+    }
 
     /**
      * Formats sorted by *real* quality, highest first.
diff --git a/app/src/main/res/values-ar/strings.xml b/app/src/main/res/values-ar/strings.xml
index 0e1cc835..dceee6da 100644
--- a/app/src/main/res/values-ar/strings.xml
+++ b/app/src/main/res/values-ar/strings.xml
@@ -486,8 +486,9 @@
     <string name="card_view">╪╣╪▒╪╢ ╪º┘ä╪¿╪╖╪º┘é╪º╪¬</string>
     <string name="show_hide">╪Ñ╪╕┘ç╪º╪▒ / ╪Ñ╪«┘ü╪º╪í</string>
     <string name="size">╪º┘ä╪¡╪¼┘à</string>
-    <string name="quality">╪º┘ä╪¼┘ê╪»╪⌐</string>
+
     <string name="date">╪º┘ä╪¬╪º╪▒┘è╪«</string>
     <string name="duration">╪º┘ä┘à╪»╪⌐</string>
     <string name="source">╪º┘ä┘à╪╡╪»╪▒</string>
+    <string name="ytdlp_missing_alert">┘à┘â╪¬╪¿╪⌐ yt-dlp ┘à┘ü┘é┘ê╪»╪⌐. ┘è╪▒╪¼┘ë ╪¬┘å╪▓┘è┘ä┘ç╪º ┘à┘å ╪º┘ä╪Ñ╪╣╪»╪º╪»╪º╪¬ ┘ä┘â┘è ┘è╪╣┘à┘ä ╪º┘ä╪¬╪╖╪¿┘è┘é.</string>
 </resources>
diff --git a/app/src/main/res/values/strings.xml b/app/src/main/res/values/strings.xml
index ecb5e4c6..1f0df1e1 100644
--- a/app/src/main/res/values/strings.xml
+++ b/app/src/main/res/values/strings.xml
@@ -496,8 +496,9 @@
     <string name="card_view">Card View</string>
     <string name="show_hide">Show / Hide</string>
     <string name="size">Size</string>
-    <string name="quality">Quality</string>
+
     <string name="date">Date</string>
     <string name="duration">Duration</string>
     <string name="source">Source</string>
+    <string name="ytdlp_missing_alert">The yt-dlp library is missing. Please download it from settings for the app to work.</string>
 </resources>
